name: Auto Reboisasi

on:
  schedule:
    # Jalan tiap hari jam 12:00 UTC (19:00 WIB)
    - cron: '0 12 * * *'
  push:
    branches:
      - main
  workflow_dispatch: # Supaya bisa di-run manual juga di tab "Actions"

permissions:
  contents: write

jobs:
  tanam_pohon:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Git Credentials
        run: |
          git config --global user.name "naidrahiqa"
          git config --global user.email "akufaqih1@gmail.com"
          
      - name: Run Reboisasi Script
        run: node index.js
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
